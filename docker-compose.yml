services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: capture
      POSTGRES_PASSWORD: capture
      POSTGRES_DB: capturewashington
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
